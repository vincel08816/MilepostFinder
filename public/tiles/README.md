# Self-hosted map tiles

Place raster tiles here using the standard slippy-map layout:

```
public/tiles/{z}/{x}/{y}.png
```

See [docs/PACKAGES.md](../docs/PACKAGES.md) for how to generate tiles for the NY Thruway corridor.

Until tiles are added, the app still runs with GeoJSON layers on a plain background.
